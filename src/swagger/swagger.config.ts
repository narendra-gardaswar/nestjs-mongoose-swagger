import { SwaggerConfig } from './swagger.interface';

export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'NestJS API',
  description: 'The NestJS API Documnentation',
  version: '1.0',
  tags: ['Auth', 'Users', 'Posts', 'Comments'],
};
